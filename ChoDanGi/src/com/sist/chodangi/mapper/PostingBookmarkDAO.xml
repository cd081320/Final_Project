<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.chodangi.seeker.IPostingBookmarkDAO">
	<!-- add -->
	<insert id="add">
		INSERT INTO POSTING_BOOKMARK(ID, POSTING_ID, S_ID, ALIAS)
		VALUES (${id}, ${posting_id}, ${s_id}, '${alias}')
	</insert>
	
	<!-- remove -->
	<delete id="remove" parameterType="int">
		DELETE
		FROM POSTING_BOOKMARK
		WHERE ID = #{id}
	</delete>
	
	<!-- search -->
	<!-- 못찾으면 0 return -->
	<select id="search" resultType="int">
		SELECT COALESCE(MAX(ID), 0) AS ID
		FROM POSTING_BOOKMARK
		WHERE S_ID = ${s_id}
		  AND POSTING_ID = ${posting_id}
	</select>
</mapper>